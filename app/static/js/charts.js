// charts.js — shared chart utilities loaded globally via base.html
// Individual chart logic lives in reports.html (inline) for co-location with its data.

Chart.defaults.font.family = "'Segoe UI', system-ui, -apple-system, sans-serif";
Chart.defaults.font.size   = 12;
Chart.defaults.color       = '#495057';
